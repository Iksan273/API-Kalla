const response = (status_code,error,data,message,res)=>{

    res.status(status_code).json({
        error:error,
        status_code: status_code,
        data:data,
        message:message
    })
}

module.exports=response